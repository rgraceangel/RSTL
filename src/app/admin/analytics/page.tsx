import type { Metadata } from "next";
import {
  Gamepad2,
  CheckCircle2,
  XCircle,
  Gift,
  Clock,
  Flame,
  AlertTriangle,
  Calendar,
} from "lucide-react";
import { StatCard } from "@/components/admin/StatCard";
import { ChartCard } from "@/components/admin/analytics/ChartCard";
import { DailyParticipationChart } from "@/component